apiVersion: v1
kind: Service
metadata:
  name: {{ include "nft-create-solana.fullname" . }}
  labels:
    {{- include "nft-create-solana.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nft-create-solana.selectorLabels" . | nindent 4 }}
