apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nft-create-solana.fullname" . }}-test-connection"
  labels:
    {{- include "nft-create-solana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nft-create-solana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
